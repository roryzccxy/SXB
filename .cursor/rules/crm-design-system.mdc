---
description: CRM 全项目统一视觉规范（风格、字体、比例、交互）
alwaysApply: true
---

# CRM 设计系统（必须遵守）

后续所有页面、组件、HTML 必须沿用本规范，禁止另起一套配色、字体或间距。视觉基准为工作台截图与 `index.html`。

## 字体

- 中文/界面：`"PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans SC", sans-serif`
- 数字/金额：继承正文字体，字重 600–700，字距略收
- 字号：辅助 12px、正文 13–14px、卡片标题 14–16px、核心指标 22–26px、侧栏 Logo 22px
- 行高：正文 1.5；标题 1.3

## 色板（CSS 变量名保持一致）

- 侧栏底 `#1f2637`，侧栏悬停 `#2a3348`，侧栏选中 `#323c55`
- Logo / 主色 `#1890ff`，主色悬停 `#40a9ff`，主色文字按钮白
- 页面底 `#eef1f6`，卡片白 `#fff`，顶栏白
- 主文字 `#1f2d3d`，次文字 `#8c8c8c`，分割线 `#e8e8e8`
- 快捷/关联图标橙 `#fa8c16`
- 成功 `#52c41a`，警告 `#faad14`，危险 `#ff4d4f`

## 比例与圆角

- 侧栏宽 220px；顶栏高 56px；内容区内边距 16px；卡片间距 16px
- 卡片圆角 8px；按钮/胶囊 6–16px；头像圆形
- 卡片阴影 `0 1px 4px rgba(0, 21, 41, 0.08)`
- 栅格：快捷操作 4 列；关联信息 4 列；中部数据概览与排行 1fr / 1fr；底部报单与待办 1fr / 1fr

## Logo

- 侧栏顶部纯文字 **CRM**，颜色 `#1890ff`，字重 700，无图形标、不加副标题
- 新页面 Logo 必须与工作台截图一致，禁止换成图标或其它文案

## 组件习惯

- 主按钮实心蓝；次按钮白底灰边
- 页内 Tab 为文字 + 底部蓝线；筛选为白底下拉
- 交互先做当前页状态切换；未交付的后续页只 toast「功能即将开放」，不要编造完整子页内容

实现时优先复用 `css/design-system.css`，不要在新文件里重写色值和字号。
